// Export all CVA variants
export {
  // Kanban Card
  kanbanCardVariants,
  type KanbanCardVariantProps,

  // Category Badge
  categoryBadgeVariants,
  type CategoryBadgeVariantProps,

  // Kanban Column
  kanbanColumnVariants,
  type KanbanColumnVariantProps,

  // Column Header Color
  columnHeaderColorVariants,
  type ColumnHeaderColorVariantProps,

  // Progress Bar
  progressBarVariants,
  progressBarFillVariants,
  type ProgressBarVariantProps,
  type ProgressBarFillVariantProps,

  // Metrics Card
  metricsCardVariants,
  type MetricsCardVariantProps,

  // Activity Icon
  activityIconVariants,
  type ActivityIconVariantProps,

  // Avatar
  avatarVariants,
  type AvatarVariantProps,

  // Page Background
  pageBackgroundVariants,
  type PageBackgroundVariantProps,

  // Main Container
  mainContainerVariants,
  type MainContainerVariantProps,
} from "./kanban.variants";
